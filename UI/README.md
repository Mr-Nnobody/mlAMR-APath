# AMR Mapping Portal UI

## Quick start

```powershell
cd UI
npm install
npm run dev
```

## API configuration

The UI expects the API at http://localhost:5170. To override:

```powershell
$env:VITE_API_BASE = "http://localhost:5170"
```
